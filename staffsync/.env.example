# Copy to .env.local for local dev overrides
# VITE_ prefix is required for Vite to expose to the browser

# Local dev only: where XAMPP serves the PHP backend (default: http://localhost/staff_cecile)
VITE_API_TARGET=http://localhost/staff_cecile
